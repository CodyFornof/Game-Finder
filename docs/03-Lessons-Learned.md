1. If your backend is showing old data - simply reboot your machine
2. To make API calls, you must have a backend - Learn more about CORS
3. You need a backend in a separate terminal to run any API calls etc. Until you get it on Railway
4. 500 internal error when postman has a 200 - could be internet, test at home or on mobile hotspot (not on public wifi)
5. Cannot find module error - means you npm installed something wrong probably
6. Put the AppContext provider in your index - if not you might get an error that it is null. This is a timing thing of rendering before it's value is given
7. If you want a variable in a logos source you have to put uri: ``
8. Keep your backend also in your Github - makes things easier when adding to online server
9. Start to finish your app in the Github folder in VS code connected to GitHub. This way one click of a button and you can push to github